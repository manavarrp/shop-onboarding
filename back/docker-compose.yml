services:
  postgres:
    image: postgres:15
    container_name: shop-onboarding-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dbshop_onboarding
    ports:
      - "5432:5432"